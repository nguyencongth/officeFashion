import { SlickCarouselDirective } from './slick-carousel.directive';

describe('SlickCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new SlickCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
